#pragma once

namespace MyLib
{
	void InitArr(float* arr, const int size);
	void PrintArr(float* arr, const int size);
	void CountDigits(float* arr, const int size);
}

#define COMPARE(a, b);
#define SIZING(a);
#define SwapINT(a, b);

void SortMass(int* array, int size);
void PrintIntArray(int* array, int size);
